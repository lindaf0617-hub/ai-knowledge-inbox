# AI Knowledge Inbox 1.7.0 Beta 1

- Approval-based Knowledge Agent with evidence pins, candidate cards, explicit approval, undo, provenance, and audit
- Causal OneDrive sync v2 with deterministic conflict handling, backups, restore, and crash recovery
- Authenticated localhost API with one-time pairing and proof-before-token service identity
- Grounded Ask with scored excerpts, clickable validated citations, browser AI, and local Ollama
- Manual update checks, store-ready extension package, and fail-closed signed stable-release policy
- 98 automated tests across Windows and macOS

**Desktop shortcuts:** Windows `Ctrl + ;` · macOS `Command + ;`

This prerelease contains clearly labeled unsigned desktop packages. Browser extensions still require Developer mode.
