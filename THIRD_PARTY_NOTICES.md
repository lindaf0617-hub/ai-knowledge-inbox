# Third-party notices

Windows release packages include the official Node.js Windows x64 executable.

Node.js is distributed under its own license. The build downloads the official runtime and includes its `LICENSE` file inside the release package.

No third-party JavaScript packages are bundled into the browser extension.
