# 黑客松项目陈述：AI Knowledge Inbox

## 一句话介绍

AI Knowledge Inbox 是一个本地优先的 AI 知识入口：把散落在 Copilot、ChatGPT、Claude、网页和桌面 Agent 中的产出一键保存，并通过可引用的 AI 问答再次组合成新的知识。

## 背景

生成式 AI 正在成为新的工作界面。我们每天让 AI 写方案、总结会议、分析客户、生成代码和解释复杂问题，但这些高价值内容通常停留在一次性会话中。

当用户真正需要复用时，常见结果是：

- 不记得内容在哪个 AI 工具里
- 找到会话，却无法快速定位结论
- 复制进笔记后丢失来源
- 相似问题再次从头提问
- 不愿把公司或个人知识上传到另一个未知云平台

AI 提高了内容生成速度，却也制造了新的“知识碎片化”。

## 我们解决的问题

我们希望把 AI 对话从一次性消费品变成长期资产。

核心体验只有两步：

```text
看到有价值的 AI 产出 → 一键保存
需要新结论 → Ask 自己的知识库
```

## 解决方案

### 1. 跨入口采集

- 浏览器：选中内容后右键保存
- Windows：复制后按 `Ctrl + ;`
- macOS：复制后按 `Command + ;`

系统保存原文、标题、来源、项目、标签和时间，并尽量保留 Markdown。

### 2. 本地优先知识库

- SQLite 本地主库
- 本地关键词与语义检索
- JSON / Markdown 可导出
- 不依赖我们的托管数据库

### 3. 用户自有云同步

知识通过用户自己的 OneDrive 同步。SQLite 不直接进入同步盘，而是生成可合并快照，并用删除墓碑防止旧数据恢复。

### 4. 可追溯的 Ask

Ask 不直接把整个知识库交给模型：

1. 本地检索相关条目
2. 只选择最相关的来源
3. 浏览器内置 AI 综合
4. 每个关键结论标注 `[K1]`、`[K2]`
5. 新回答可以再次保存

这让 AI 从“回答问题的工具”变成“使用个人知识的接口”。

## 为什么现在

AI Agent 正在快速增长，但 Agent 要真正工作，必须获得持续、可信、可控的上下文。今天的会话历史不是好的长期记忆；传统笔记也不是为 Agent 设计的知识接口。

AI Knowledge Inbox 位于两者之间：

> 向上连接所有 AI 和 Agent，向下连接用户真正拥有的知识。

## 创新点

1. **跨 AI 工具统一入口**：不依赖单一模型平台。
2. **本地优先 + OneDrive**：兼顾隐私、可迁移和多设备。
3. **引用式个人 RAG**：回答不是黑盒，用户能看到依据。
4. **知识闭环**：AI 互动产生的新结论可以重新进入知识库。
5. **Agent-ready**：未来可作为分析 Agent 的权限化记忆层。

## 开发过程

项目从 4 小时单页 MVP 开始，逐步验证并扩展：

1. 粘贴、保存、列表、搜索
2. 浏览器右键采集
3. 项目、标签、去重和 Markdown
4. Windows 桌面快捷键
5. 共享 SQLite
6. OneDrive 多设备合并
7. 本地语义搜索
8. 引用式 Ask
9. 中英双语与 macOS
10. 可复现公开发布

每一步都先解决真实摩擦，而不是先搭建庞大后端。

## 技术实现

- Browser Extension：Manifest V3、原生 JavaScript
- Desktop：PowerShell/WinForms（Windows）、Swift/AppKit（macOS）
- Local API：Node.js 原生 HTTP + SQLite
- Storage：SQLite + OneDrive JSON merge snapshot
- Retrieval：关键词权重 + 本地轻量向量
- AI：浏览器 Prompt API
- Distribution：GitHub Actions、Releases、Pages

## 隐私与安全

- 服务只监听 localhost
- 普通网页无法访问本地 API
- 无遥测、广告或托管知识后端
- Markdown 不使用 `innerHTML`
- 知识来源按不可信数据处理，禁止执行其中提示
- 自动写库将在未来采用审批和来源记录

## 用户价值

### 即时价值

- 2 秒保存有价值内容
- 统一检索不同 AI 的产出
- 减少重复提问和重复调研

### 长期价值

- 建立个人 AI 资产
- 形成可追溯的决策与研究脉络
- 为 Agent 提供持续上下文
- 从“使用 AI”升级为“拥有自己的 AI 知识系统”

## 演示流程

1. 在 Copilot 中生成一段方案
2. 复制并按快捷键保存
3. 在网页中选中另一条资料并右键保存
4. 打开知识库，看到统一的项目、标签和来源
5. 提问“整合 Agent 安全方案并给出行动清单”
6. 展示本地检索来源和 `[K1]` 引用回答
7. 将回答保存回知识库
8. 展示 OneDrive 同步文件和另一设备更新

## 商业与扩展潜力

- 免费个人版：本地知识库、浏览器和桌面采集
- Pro：高级模型、Obsidian/Notion 连接器、自动工作流
- Enterprise：Entra ID、SharePoint、权限继承、敏感标签和审计
- Agent Platform：为任务 Agent 提供可授权的知识读写 API

## 路线图

近期：

- 商店上架与签名安装包
- Ollama / Azure OpenAI 可选适配
- Obsidian Vault 一键导出

下一阶段：

- Knowledge Agent
- 自动发现缺失信息
- 用户审批后补充材料
- 带来源、版本和置信度的新知识写入

## 结语

AI 的下一阶段不只是生成更多内容，而是让生成过的内容能够被记住、被验证、被重新组合。

AI Knowledge Inbox 希望成为每个用户自己的 AI 知识底座。
